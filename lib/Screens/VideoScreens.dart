import 'package:flutter/material.dart';

class VideoScreens extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text('It is a Video Screens',
            style: TextStyle(fontSize: 32.0),
          ),
      ),
    );
  }
}