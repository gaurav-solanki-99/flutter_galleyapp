import 'package:flutter/material.dart';

class AudioScreens extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text('Its is a Audio Screens',
            style: TextStyle(fontSize: 32.0),
          )
      ),
    );
  }
}