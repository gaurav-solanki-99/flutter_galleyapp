import 'package:flutter/material.dart';

class ImageScreens  extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text('It is a Image Screens ',
            style: TextStyle(fontSize: 32.0),
          )
      ),
    );
  }
}